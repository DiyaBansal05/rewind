services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: recordingportal
      POSTGRES_USER: recordingportal
      POSTGRES_PASSWORD: recordingportal
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
